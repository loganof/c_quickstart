#include <string>
#include <iostream>

int main(int argc, char const *argv[])
{
    std::string str = "hello";
    std::string substr = str.substr(0, 2);
    std::cout << atoi("131") << std::endl;
    return 0;
}
